(function() {
  // Initialize any page-wide logic if needed. The individual pages have their own scripts.
  // This file exists to ensure all modules are loaded and to attach the global App namespace.
  window.App = window.App || {};
})();
